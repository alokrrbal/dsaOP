// you need to implement a queue data structure with a list using plain list methods. You can use list.append()and list.pop() in the question.
// Note - You can't uselist.pop(index) too in this question

//Input -> (E 3 ) means add 3 in the queue
    //  -> (D) means to remove the front element of the queue and print it one the screen
    // Note- If the queue is empty print - `Empty` (without quotes)

    // Sample Input   Sample Output

    // E 2            2
    // E 3            3
    // D
    // D


    // Solution

// let que=[]

// function listAndQue(arr){
//     if(arr[0]=="E"){
//         que.push(arr[1])
//     }else{
//         if(que.length==0){
//             console.log("Empty")
//         }else{
//             console.log(que[0])
//             que.shift()
//         }
//     }
// }